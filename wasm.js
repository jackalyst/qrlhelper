// const QRLLIB = require('./node_modules/qrllib/build/web-libjsqrl.js')

// const buf = fs.readFileSync('./node_modules/qrllib/build/web-libjsqrl.wasm');
// const qrllib = Promise WebAssembly.instantiate(toUint8Array(buf)).exports;

// function toUint8Array(buf) {
//   var u = new Uint8Array(buf.length);
//   for (var i = 0; i < buf.length; ++i) {
//     u[i] = buf[i];
//   }
//   return u;
// }
